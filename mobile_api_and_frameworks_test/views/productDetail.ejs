<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Meta tags for character set and responsive design -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Dynamic title based on the product name -->
    <title><%= product.name %> - Product Details</title>

    <!-- Internal CSS styles for the page -->
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f1f1f1;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
        }

        h1 {
            color: #333;
        }

        p {
            color: #666;
        }

        /* Toolbar styles */
        .toolbar {
            background-color: #f1f1f1;
            padding: 10px 0;
            text-align: center;
        }

        .toolbar a {
            margin-right: 20px;
            text-decoration: none;
            color: #333;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <!-- Toolbar section for navigation links -->
    <div class="toolbar">
        <a href="/">Home</a>
        <a href="/products">Products</a>
        <a href="/new-products">New Products</a>
    </div>

    <!-- Main content container with product details -->
    <div class="container">
        <!-- Product name displayed as the main heading -->
        <h1><%= product.name %></h1>
        <!-- Product ID displayed as a paragraph -->
        <p>Product ID: <%= product.id %></p>
        <!-- Additional product details can be added here as needed -->
    </div>
</body>

</html>
